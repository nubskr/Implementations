#include <bits/stdc++.h>
using namespace std;
#define        pb                push_back
#define        int               long long
#define        nl                '\n'
#define        all(x)            (x).begin(),(x).end()
#define        se                second
#define        fs                first
#define        rall(x)           (x).rbegin(),(x).rend()
#define        make_unique(vec)  sort(all(vec)); vec.erase(unique(vec.begin(),vec.end()),vec.end());
const int MOD = 1e9 + 7;
#ifdef DEBUG
#include</home/nubskr/debug.h>
#else
#define deb(...) 44
#define debv(a) 44
#define debp(a) 44
#endif


int32_t main(){ios::sync_with_stdio(0); cin.tie(0); // always trust your intuition
auto solve = [&]() {
    
};

int t = 1;
cin >> t;

while(t--){solve();}
cerr << "\nTime Elapsed: " << 1000 * clock() / CLOCKS_PER_SEC << "ms\n";
}
